

mvn archetype:generate -Dversion=1.0 -DarchetypeArtifactId=maven-archetype-quickstart -DgroupId=org.ravi.driver.queries -DartifactId=driver-queries


Most of these are test(s) and code that I encountered and problems that got asked at Interviews. I tried
to convert most of them into a valid-junit test that asserts something. There may be some violators still

Most of the code, I indicated the source of the code/algorithm/inspiration.
